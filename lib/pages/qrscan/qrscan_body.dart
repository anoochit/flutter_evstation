import 'package:flutter/material.dart';

class QRScanBody extends StatelessWidget {
  const QRScanBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("QR Scan"),
      ),
    );
  }
}
